const optimizeRoutes = async (orders) => {
}

const handleGenerateMapLink = async (orders) => {
    const routesOptimized = [];

}


module.exports = {
    optimizeRoutes, 
    handleGenerateMapLink
}